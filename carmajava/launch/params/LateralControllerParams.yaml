# LateralControllerParams.yaml
# Defines the ros parameters which are required by the Lateral Controller

# Double: Angle threshold over which an axes angle command is considered a turn.
# Radians
angle_threshold: 0.0349066 # 2 deg
